export default class User{
    constructor(name,dayOfTracking,currentBalance,id)
    {
        this.name=name
        this.dayOfTracking=dayOfTracking;
        this.currentBalance=currentBalance;
        this.id=id;
    }
}